# decoze admin app

The Flutter Web admin dashboard. Part of the `decoze` Flutter e-commerce template monorepo — see the [root README](../../README.md) for the full setup guide (Firebase project, granting a user admin access, security rules, rebranding).

```bash
flutter run -d chrome
```
